# Research Report: Task #479

**Task**: 479 - fix_remaining_nvim_refs_in_core_ext
**Started**: 2026-04-18T12:00:00Z
**Completed**: 2026-04-18T12:05:00Z
**Effort**: small
**Dependencies**: None (task 478 completed)
**Sources/Inputs**:
- Codebase grep/read of `.claude/extensions/core/` source files
- Codebase grep/read of `.claude/` deployed copies
**Artifacts**:
- `specs/479_fix_remaining_nvim_refs_in_core_ext/reports/01_nvim-refs-audit.md`
**Standards**: report-format.md

## Executive Summary

- Found 6 distinct issues across 8 source files (plus their deployed copies)
- All source files live under `.claude/extensions/core/` and each has a deployed copy under `.claude/`
- Issue 1: "neotex extension loader" in template header (3 locations: source, deployed, generated CLAUDE.md)
- Issue 2: "Neovim Lua loader" references in extension-development.md and extension-system.md (source + deployed)
- Issue 3: "neovim" in example extension lists in 3 files (system-overview x2, merge-sources/claudemd)
- Issue 4: meta-guide.md has duplicate `.claude/README.md, .claude/README.md` instead of `.claude/docs/README.md, .claude/README.md`
- Total: 12 file edits needed (6 source + 6 deployed copies)

## Context & Scope

Task 478 cleaned `<leader>ac` references from core extension files but missed other nvim/neotex-specific language. This audit identifies the exact lines and proposed replacements for each remaining reference.

## Findings

### Issue 1: "neotex extension loader" in template header

**Source**: `.claude/extensions/core/templates/claudemd-header.md` line 5
**Deployed copies**: `.claude/templates/claudemd-header.md` line 5, `.claude/CLAUDE.md` line 5

Current line:
```
<!-- Generated by: neotex extension loader -->
```

Proposed replacement:
```
<!-- Generated by: extension loader -->
```

Note: `.claude/CLAUDE.md` is regenerated from the template, so fixing the template source and deployed copy is sufficient. CLAUDE.md will be updated on next reload.

### Issue 2: "Neovim Lua loader" in extension-development.md

**Source**: `.claude/extensions/core/context/guides/extension-development.md` line 11
**Deployed**: `.claude/context/guides/extension-development.md` line 11

Current line:
```
The extension system splits across a Neovim Lua loader (Layer 1) that manages which files exist in the `.claude/` runtime, and the `.claude/` agent system (Layer 2) that Claude Code reads. The loader copies files from extension sources into the runtime on load, merges context index entries, and calls `generate_claudemd()` to rebuild `.claude/CLAUDE.md`. On unload, it removes those files and regenerates. Claude Code has no knowledge of the extension system itself -- it only sees the resulting runtime.
```

Proposed replacement (change only "a Neovim Lua loader" to "an extension loader"):
```
The extension system splits across an extension loader (Layer 1) that manages which files exist in the `.claude/` runtime, and the `.claude/` agent system (Layer 2) that Claude Code reads. The loader copies files from extension sources into the runtime on load, merges context index entries, and calls `generate_claudemd()` to rebuild `.claude/CLAUDE.md`. On unload, it removes those files and regenerates. Claude Code has no knowledge of the extension system itself -- it only sees the resulting runtime.
```

### Issue 2b: "Neovim Lua Loader" in extension-system.md box drawing

**Source**: `.claude/extensions/core/docs/architecture/extension-system.md` lines 45, 54
**Deployed**: `.claude/docs/architecture/extension-system.md` lines 45, 54

Current lines:
```
│ Layer 1: Neovim Lua Loader                                      │
...
│  Trigger: Extension picker UI in Neovim                        │
```

Proposed replacements:
```
│ Layer 1: Extension Loader                                       │
...
│  Trigger: Extension picker UI (editor-specific)                │
```

Note: Box-drawing alignment must be preserved. The box is 65 characters wide internally.

### Issue 3a: "neovim" in system-overview.md (context/architecture)

**Source**: `.claude/extensions/core/context/architecture/system-overview.md` line 387
**Deployed**: `.claude/context/architecture/system-overview.md` line 387

Current line:
```
**Note**: Extensions (e.g., neovim, lean4, latex, typst) add task type routing entries. See `.claude/extensions/*/manifest.json`.
```

Proposed replacement (use editor-agnostic examples):
```
**Note**: Extensions (e.g., nix, lean4, latex, typst) add task type routing entries. See `.claude/extensions/*/manifest.json`.
```

### Issue 3b: "neovim" in system-overview.md (docs/architecture)

**Source**: `.claude/extensions/core/docs/architecture/system-overview.md` line 198
**Deployed**: `.claude/docs/architecture/system-overview.md` line 198

Current line:
```
**Note**: Additional task types (neovim, latex, typst, python, etc.) are available via extensions in `.claude/extensions/`.
```

Proposed replacement:
```
**Note**: Additional task types (nix, latex, typst, python, etc.) are available via extensions in `.claude/extensions/`.
```

### Issue 3c: "neovim" in merge-sources/claudemd.md

**Source**: `.claude/extensions/core/merge-sources/claudemd.md` line 73
**Deployed**: Generated into `.claude/CLAUDE.md` line 80

Current line:
```
Extensions provide additional task type support (neovim, lean4, latex, typst, python, nix, web, z3, epi, formal, founder, present, etc.). See `.claude/extensions/*/manifest.json` for available extensions and their capabilities.
```

Proposed replacement (remove "neovim," since it is project-specific, not core):
```
Extensions provide additional task type support (lean4, latex, typst, python, nix, web, z3, epi, formal, founder, present, etc.). See `.claude/extensions/*/manifest.json` for available extensions and their capabilities.
```

Note: This merge source has no separate deployed copy -- it generates into CLAUDE.md which is rebuilt on reload.

### Issue 4: Duplicate path in meta-guide.md

**Source**: `.claude/extensions/core/context/meta/meta-guide.md` line 450
**Deployed**: `.claude/context/meta/meta-guide.md` line 450

Current line:
```
- **Documentation**: `.claude/README.md`, `.claude/README.md`
```

Proposed replacement (restore the correct first path):
```
- **Documentation**: `.claude/docs/README.md`, `.claude/README.md`
```

### Additional findings: project-overview.md

**Location**: `.claude/context/repo/project-overview.md` line 11

This file also contains "Neovim Lua loader" but it is NOT a core extension file -- it is project-specific context. It correctly describes the Neovim-specific loader in this repository. This reference should NOT be changed as part of this task since project-overview.md is project-specific, not part of the editor-agnostic core.

## Decisions

- Replace "neovim" in example lists with "nix" (already a core extension, not project-specific)
- Do not touch project-overview.md (project-specific, not core)
- Do not touch `.claude/CLAUDE.md` directly (regenerated from templates/merge-sources on reload)
- For extension-system.md box drawing, must carefully preserve character alignment

## Risks & Mitigations

- **Box drawing alignment**: The extension-system.md changes involve box-drawing characters with fixed-width alignment. Implementation must count characters carefully to maintain box integrity.
- **Regeneration**: `.claude/CLAUDE.md` is regenerated on extension reload. Fixing the merge-sources and template is sufficient; no direct CLAUDE.md edit needed. However, the deployed `templates/claudemd-header.md` should be updated to prevent stale header until next reload.

## Appendix

### File edit summary

| # | Source file | Deployed copy | Issue |
|---|------------|---------------|-------|
| 1 | `extensions/core/templates/claudemd-header.md:5` | `templates/claudemd-header.md:5` | "neotex extension loader" |
| 2 | `extensions/core/context/guides/extension-development.md:11` | `context/guides/extension-development.md:11` | "Neovim Lua loader" |
| 3 | `extensions/core/docs/architecture/extension-system.md:45,54` | `docs/architecture/extension-system.md:45,54` | "Neovim Lua Loader", "in Neovim" |
| 4 | `extensions/core/context/architecture/system-overview.md:387` | `context/architecture/system-overview.md:387` | "neovim" in example list |
| 5 | `extensions/core/docs/architecture/system-overview.md:198` | `docs/architecture/system-overview.md:198` | "neovim" in example list |
| 6 | `extensions/core/merge-sources/claudemd.md:73` | (generates CLAUDE.md) | "neovim" in example list |
| 7 | `extensions/core/context/meta/meta-guide.md:450` | `context/meta/meta-guide.md:450` | duplicate `.claude/README.md` |

All paths relative to `.claude/`.
